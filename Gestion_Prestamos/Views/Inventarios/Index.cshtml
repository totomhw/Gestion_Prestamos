@model IEnumerable<Gestion_Prestamos.Models.Inventario>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UnidadesTotales)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UnidadesDisponibles)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Elemento.Nombre)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UnidadesTotales)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UnidadesDisponibles)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Elemento.Nombre)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.InventarioId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.InventarioId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.InventarioId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
